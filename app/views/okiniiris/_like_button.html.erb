
  <% if current_user.like?(micropost) %>
    <%= form_with(model: current_user.okiniiris.find_by(micropost_id: micropost.id), method: :delete) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Unlike', class: 'btn btn-danger w-100' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.okiniiris.build) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Like', class: 'btn btn-primary w-100' %>
    <% end %>
  <% end %>
